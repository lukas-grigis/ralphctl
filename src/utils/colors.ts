import { cyan, green, red, yellow, blue, gray, bold, dim } from 'colorette';

// Re-export colorette functions
export { cyan, green, red, yellow, blue, gray, bold, dim };

// Semantic aliases for consistent styling
export const success = green;
export const error = red;
export const warning = yellow;
export const info = blue;
export const muted = gray;
export const highlight = cyan;
export const accent = bold;
export const subtle = dim;
