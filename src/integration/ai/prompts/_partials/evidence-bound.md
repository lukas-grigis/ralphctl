<evidence-bound>
Bound how much command output you carry: quote the decisive lines — roughly the last 50 per
command — rather than the full log. When a command's output exceeds that, write the complete log
to a file in your session working directory, never inside the repository, and cite its path
instead of pasting the rest.
</evidence-bound>
